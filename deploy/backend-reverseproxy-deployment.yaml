apiVersion: apps/v1
kind: Deployment
metadata:
  name: reverseproxy
  labels:
    app: reverseproxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reverseproxy
  template:
    metadata:
      labels:
        app: reverseproxy
    spec:
      containers:
      - name: reverseproxy
        image: papiknomic/reverseproxy:v1
        imagePullPolicy: Always          
        resources:
          requests:
            memory: "100Mi"
            cpu: "250m"
          limits:
            memory: "1024Mi"
            cpu: "1000m" 
        ports:
        - containerPort: 8080
        
